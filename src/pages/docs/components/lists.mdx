import { getProps } from "@utils";
import BasicUsage from "@components/docs/components/basic-usage/basic-usage.mdx";
import Installation from "@components/docs/components/installation/installation.mdx";

export const getStaticProps = async () => {
  const { type, html, demoCode, sourceCode } = await getProps({
    type: "list"
  }) 

  return {
    props: {
      ssg: {
        type,
        html,
        demoCode,
        sourceCode,
      },
    },
  };
};

# Lists

Organize information with simple lists

## Basic Usage

<BasicUsage />

## Installation

<Installation cli="list" />

## Examples

Coming soon
